<%= form_for Group.new do |f| %>
  <div class="modal-header">
    <h5 class="modal-title" id="addClassLabel">Dodaj klasę</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Cofnij">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
  <div class="modal-body">
    <div class="form-group">
      <%= f.label "Podaj oznaczenie klasy" %>
      <%= f.text_area(:groupName, {class: 'form-control', id: 'classContent', 'rows': '1'}) %>
    </div>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cofnij</button>
    <%= f.submit(value: "Dodaj klasę", class: 'btn btn-primary', 'data-disable-with': "Dodaję klasę...", method: :post) %>
  </div>
<% end %>